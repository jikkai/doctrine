import { defineDirectory } from '@amamo/doctrine'

export default defineDirectory({
  icon: 'BookOpen',
  items: [{ icon: 'Rocket', page: 'getting-started', title: 'Getting started' }],
  title: 'Guide',
})
